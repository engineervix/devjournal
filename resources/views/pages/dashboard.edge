@component('layouts/main')
  @slot('main')
    <div class="mb-6 flex justify-between items-center">
      <h1 class="text-2xl font-bold">Dashboard</h1>
      <div class="text-sm">
        {{ formatDateWithDay(currentDate, 'short') }}
      </div>
    </div>

    {{-- Quick Stats --}}
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
      <div class="bg-white dark:bg-gray-800 shadow rounded-lg p-4">
        <h2 class="text-sm font-medium text-gray-500 dark:text-gray-400">Entries Today</h2>
        <p class="text-2xl font-semibold">{{ stats.todayCount }}</p>
      </div>

      <div class="bg-white dark:bg-gray-800 shadow rounded-lg p-4">
        <h2 class="text-sm font-medium text-gray-500 dark:text-gray-400">Current Streak</h2>
        <p class="text-2xl font-semibold">{{ stats.streak }} days</p>
      </div>

      <div class="bg-white dark:bg-gray-800 shadow rounded-lg p-4">
        <h2 class="text-sm font-medium text-gray-500 dark:text-gray-400">Total Entries</h2>
        <p class="text-2xl font-semibold">{{ stats.totalCount }}</p>
      </div>

      <div class="bg-white dark:bg-gray-800 shadow rounded-lg p-4">
        <h2 class="text-sm font-medium text-gray-500 dark:text-gray-400">Most Used Tag</h2>
        @if(stats.topTag)
          <p class="text-2xl font-semibold">{{ stats.topTag.name }}</p>
        @else
          <p class="text-2xl font-semibold text-gray-400 dark:text-gray-500">—</p>
        @end
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {{-- Today's Entries --}}
      <div class="md:col-span-2">
        <h2 class="text-xl font-semibold mb-4">Today's Entries</h2>

        @if(!hasDailyLog)
          <div class="bg-yellow-50 dark:bg-yellow-900/20 border border-yellow-200 dark:border-yellow-800 rounded-lg p-4 mb-4">
            <div class="flex">
              <div class="flex-shrink-0">
                <i class="ph ph-warning text-yellow-400"></i>
              </div>
              <div class="ml-3">
                <p class="text-sm text-yellow-700 dark:text-yellow-200">
                  You haven't created a daily log today.
                  <a href="{{ route('entries.create') }}?type=daily" class="font-medium underline hover:text-yellow-600 dark:hover:text-yellow-100">
                    Create one now
                  </a>
                </p>
              </div>
            </div>
          </div>
        @end

        @if(todayEntries.length > 0)
          @each(entry in todayEntries)
            @!component('components/entry-card', { entry })
          @end
        @else
          <div class="bg-white dark:bg-gray-800 shadow rounded-lg p-8 text-center">
            <i class="ph ph-notebook text-4xl text-gray-400 dark:text-gray-500 mb-3"></i>
            <h3 class="text-lg font-medium text-gray-500 dark:text-gray-400 mb-1">No entries today</h3>
            <p class="text-gray-500 dark:text-gray-400 mb-4">Start tracking your development journey.</p>
            <a href="{{ route('entries.create') }}" class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
              Create first entry
            </a>
          </div>
        @end
      </div>

      {{-- Recent Achievements & Tags --}}
      <div class="md:col-span-1">
        {{-- Recent Achievements --}}
        <h2 class="text-xl font-semibold mb-4">Recent Achievements</h2>

        @if(achievements.length > 0)
          @each(achievement in achievements)
            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow rounded-lg mb-4 p-4">
              <div class="flex items-center mb-2">
                <span class="text-pink-500 mr-2">
                  <i class="ph ph-medal"></i>
                </span>
                <h3 class="font-medium">{{ achievement.title || 'Untitled Achievement' }}</h3>
              </div>
              <p class="text-sm text-gray-500 dark:text-gray-400 mb-1 truncate">
                {{ formatDateWithDay(achievement.createdAt, 'medium') }}
              </p>
              <a href="{{ route('entries.show', { id: achievement.id }) }}" class="text-sm text-indigo-600 dark:text-indigo-400 hover:underline">
                View details
              </a>
            </div>
          @end
        @else
          <div class="bg-white dark:bg-gray-800 shadow rounded-lg p-4 text-center">
            <p class="text-gray-500 dark:text-gray-400 mb-2">No achievements recorded yet.</p>
            <a href="{{ route('entries.create') }}?type=achievement" class="text-sm text-indigo-600 dark:text-indigo-400 hover:underline">
              Record an achievement
            </a>
          </div>
        @end

        {{-- Popular Tags --}}
        <h2 class="text-xl font-semibold mt-6 mb-4">Popular Tags</h2>

        @if(popularTags.length > 0)
          <div class="bg-white dark:bg-gray-800 shadow rounded-lg p-4">
            <div class="flex flex-wrap gap-2">
              @each(tag in popularTags)
                <a href="{{ route('entries.byTag', { slug: tag.slug }) }}"
                  class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-indigo-100 text-indigo-800 dark:bg-indigo-900 dark:text-indigo-200 hover:bg-indigo-200 dark:hover:bg-indigo-800">
                  {{ tag.name }}
                  <span class="ml-1 text-indigo-600 dark:text-indigo-300">{{ tag.usageCount }}</span>
                </a>
              @end
            </div>
            <div class="mt-3 pt-3 border-t border-gray-200 dark:border-gray-700">
              <a href="{{ route('tags.index') }}" class="text-sm text-indigo-600 dark:text-indigo-400 hover:underline">
                View all tags →
              </a>
            </div>
          </div>
        @else
          <div class="bg-white dark:bg-gray-800 shadow rounded-lg p-4 text-center">
            <p class="text-gray-500 dark:text-gray-400">No tags yet.</p>
          </div>
        @end
      </div>
    </div>
  @end
@end
