<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="csrf-token" content="{{ csrfToken }}">
  <title>{{ title ?? 'DevJournal' }}</title>

    {{-- Favicons --}}
    <link rel="apple-touch-icon" sizes="180x180" href="/images/ico/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/images/ico/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/images/ico/favicon-16x16.png" />
    <link rel="manifest" href="/images/ico/site.webmanifest" />

    {{-- OpenGraph Meta Tags --}}
    <meta property="og:title" content="{{ title ?? 'DevJournal' }}" />
    <meta
      property="og:description"
      content="The developer's journal for daily logs, learning notes, and career achievements. Self-hosted, keyboard-driven, built for engineering growth."
    />
    <meta
      property="og:image"
      content="{{ request.protocol() }}://{{ request.host() }}/images/illustrations/og-image.jpg"
    />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="en_ZM" />
    <meta property="og:locale:alternate" content="en_GB" />
    {{-- Twitter specific --}}
    <meta name="twitter:card" content="summary_large_image" />

  {{-- Prevent FOUC by initializing theme immediately --}}
  <script>
    // Initialize theme before any content renders to prevent flashing
    (function() {
      const storedTheme = localStorage.getItem('theme');
      const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      const theme = storedTheme || (systemPrefersDark ? 'dark' : 'light');

      if (theme === 'dark') {
        document.documentElement.classList.add('dark');
      }
    })();
  </script>



  {{-- Vite CSS and JS --}}
  @vite(['resources/css/app.css', 'resources/js/app.js'])

  {{-- Prevent Alpine.js FOUC --}}
  <style>
    [x-cloak] { display: none !important; }

    /* Critical CSS to prevent layout shift */

    /* Ensure dark mode styles are applied immediately */
    .dark {
      color-scheme: dark;
    }

    /* Prevent theme icon flashing */
    .theme-icon-light.dark\:hidden {
      display: none;
    }

    .dark .theme-icon-dark.hidden {
      display: none;
    }

    .dark .theme-icon-dark.dark\:block {
      display: block;
    }
  </style>
</head>
<body class="bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-gray-100 min-h-screen flex flex-col"{{ auth.user ? ' data-new-entry-route="' + route('entries.create') + '"' : '' }}>
  {{-- Header --}}
  <header class="bg-white dark:bg-gray-800 shadow">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center py-4">
        <div class="flex items-center">
          <a href="{{ route('home') }}" class="font-bold text-xl text-indigo-600 dark:text-indigo-400">DevJournal</a>
        </div>

        {{-- Main Navigation --}}
        @if(auth.user)
        <nav class="hidden md:flex items-center space-x-8">
          <a href="{{ route('home') }}"
             class="text-gray-700 dark:text-gray-300 hover:text-indigo-600 dark:hover:text-indigo-400 px-3 py-2 rounded-md text-sm font-medium transition-colors {{ request.matchesRoute('home') ? 'text-indigo-600 dark:text-indigo-400 bg-indigo-50 dark:bg-indigo-900/20' : '' }}">
            <i class="ph ph-house mr-1"></i>
            Dashboard
          </a>

          <a href="{{ route('entries.index') }}"
             class="text-gray-700 dark:text-gray-300 hover:text-indigo-600 dark:hover:text-indigo-400 px-3 py-2 rounded-md text-sm font-medium transition-colors {{ request.matchesRoute('entries.index') ? 'text-indigo-600 dark:text-indigo-400 bg-indigo-50 dark:bg-indigo-900/20' : '' }}">
            <i class="ph ph-notebook mr-1"></i>
            All Entries
          </a>

          <a href="{{ route('tags.index') }}"
             class="text-gray-700 dark:text-gray-300 hover:text-indigo-600 dark:hover:text-indigo-400 px-3 py-2 rounded-md text-sm font-medium transition-colors {{ request.matchesRoute('tags.index') ? 'text-indigo-600 dark:text-indigo-400 bg-indigo-50 dark:bg-indigo-900/20' : '' }}">
            <i class="ph ph-tag mr-1"></i>
            Tags
          </a>
        </nav>
        @endif

        <div class="flex items-center gap-4">
          {{-- Mobile menu button --}}
          @if(auth.user)
          <div class="md:hidden" x-data="mobileMenu" x-cloak>
            <button @click="toggle" class="p-2 rounded-md text-gray-400 hover:text-gray-500 hover:bg-gray-100 dark:hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-indigo-500">
              <span class="sr-only">Open main menu</span>
              <i class="ph ph-list text-xl" x-show="!open"></i>
              <i class="ph ph-x text-xl" x-show="open" x-cloak></i>
            </button>

            {{-- Mobile menu --}}
            <div x-show="open"
                 @click.away="close"
                 x-transition:enter="transition ease-out duration-100"
                 x-transition:enter-start="transform opacity-0 scale-95"
                 x-transition:enter-end="transform opacity-100 scale-100"
                 x-transition:leave="transition ease-in duration-75"
                 x-transition:leave-start="transform opacity-100 scale-100"
                 x-transition:leave-end="transform opacity-0 scale-95"
                 class="absolute top-16 right-4 left-4 bg-white dark:bg-gray-800 rounded-md shadow-lg z-50 py-2"
                 x-cloak>
              <a href="{{ route('home') }}" class="block px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700">
                <i class="ph ph-house mr-2"></i>Dashboard
              </a>
              <a href="{{ route('entries.index') }}" class="block px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700">
                <i class="ph ph-notebook mr-2"></i>All Entries
              </a>
              <a href="{{ route('tags.index') }}" class="block px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700">
                <i class="ph ph-tag mr-2"></i>Tags
              </a>
              <div class="border-t border-gray-200 dark:border-gray-700 my-2"></div>
              <div class="px-4 py-2">
                <form action="{{ route('entries.search') }}" method="GET">
                  <div class="relative">
                    <span class="absolute inset-y-0 left-0 flex items-center pl-2">
                      <i class="ph ph-magnifying-glass text-gray-400"></i>
                    </span>
                    <input
                      type="text"
                      name="q"
                      placeholder="Search entries..."
                      class="w-full py-2 pl-8 pr-4 bg-gray-100 dark:bg-gray-700 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 text-sm"
                    />
                  </div>
                </form>
              </div>
            </div>
          </div>
          @endif

          {{-- Search Bar --}}
          @if(auth.user)
          <form action="{{ route('entries.search') }}" method="GET" class="hidden md:block">
            <div class="relative text-gray-500 dark:text-gray-400">
              <span class="absolute inset-y-0 left-0 flex items-center pl-2">
                <i class="ph ph-magnifying-glass"></i>
              </span>
              <input
                type="text"
                name="q"
                placeholder="Search entries..."
                class="py-2 pl-8 pr-4 bg-gray-100 dark:bg-gray-700 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 w-64"
              />
            </div>
          </form>
          @endif

          {{-- User menu/settings --}}
          @if(auth.user)
          <div class="relative" x-data="userMenu" x-cloak>
            <button @click="toggle" class="flex items-center focus:outline-none">
              <span class="hidden md:block mr-2">{{ auth.user.fullName ?? auth.user.email }}</span>
              <div class="w-8 h-8 rounded-full bg-indigo-500 flex items-center justify-center text-white">
                {{ auth.user.fullName ? auth.user.fullName.charAt(0).toUpperCase() : auth.user.email.charAt(0).toUpperCase() }}
              </div>
            </button>

            <div x-show="open"
                 @click.away="close"
                 x-transition:enter="transition ease-out duration-100"
                 x-transition:enter-start="transform opacity-0 scale-95"
                 x-transition:enter-end="transform opacity-100 scale-100"
                 x-transition:leave="transition ease-in duration-75"
                 x-transition:leave-start="transform opacity-100 scale-100"
                 x-transition:leave-end="transform opacity-0 scale-95"
                 class="absolute right-0 mt-2 w-48 py-2 bg-white dark:bg-gray-800 rounded-md shadow-lg z-50"
                 x-cloak>
              <a href="#" class="block px-4 py-2 text-sm hover:bg-gray-100 dark:hover:bg-gray-700">Profile</a>
              <a href="#" class="block px-4 py-2 text-sm hover:bg-gray-100 dark:hover:bg-gray-700">Settings</a>
              <form action="{{ route('auth.logout') }}" method="POST">
                {{ csrfField() }}
                <button type="submit" class="w-full text-left block px-4 py-2 text-sm text-red-600 dark:text-red-400 hover:bg-gray-100 dark:hover:bg-gray-700">
                  Logout
                </button>
              </form>
            </div>
          </div>
          @else
          {{-- Login link for unauthenticated users --}}
          <a href="{{ route('auth.login.show') }}" class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
            <i class="ph ph-sign-in mr-2"></i>
            Login
          </a>
          @endif

          {{-- Theme toggle --}}
          <button id="theme-toggle" class="p-2 rounded-md hover:bg-gray-100 dark:hover:bg-gray-700 text-gray-700 dark:text-gray-300 flex items-center justify-center">
            <i class="ph ph-moon theme-icon-light text-gray-700 text-lg"></i>
            <i class="ph ph-sun theme-icon-dark text-gray-300 text-lg hidden"></i>
          </button>
        </div>
      </div>
    </div>
  </header>

  {{-- Main Content --}}
  <main class="flex-1 max-w-7xl w-full mx-auto px-4 sm:px-6 lg:px-8 py-6">
    {{-- Display Flash Messages --}}
    @component('components/flash_messages')
    @end

    {{{ await $slots.main() }}}
  </main>

  {{-- Quick Entry Button --}}
  @if(auth.user)
  <div class="fixed right-6 bottom-6 z-50" x-data="{ showTooltip: false }">
    <a href="{{ route('entries.create') }}"
       @mouseenter="showTooltip = true"
       @mouseleave="showTooltip = false"
       class="group flex items-center justify-center w-16 h-16 bg-gradient-to-r from-indigo-600 to-purple-600 hover:from-indigo-700 hover:to-purple-700 text-white rounded-full shadow-xl hover:shadow-2xl transform hover:scale-110 transition-all duration-200 focus:outline-none focus:ring-4 focus:ring-indigo-500/50 focus:ring-offset-2 dark:focus:ring-offset-gray-900">
      <i class="ph ph-plus text-2xl group-hover:rotate-90 transition-transform duration-200"></i>
    </a>

    {{-- Tooltip --}}
    <div x-show="showTooltip"
         x-transition:enter="transition ease-out duration-200"
         x-transition:enter-start="opacity-0 transform scale-95"
         x-transition:enter-end="opacity-100 transform scale-100"
         x-transition:leave="transition ease-in duration-150"
         x-transition:leave-start="opacity-100 transform scale-100"
         x-transition:leave-end="opacity-0 transform scale-95"
         class="absolute right-20 bottom-4 px-3 py-2 bg-gray-900 dark:bg-gray-700 text-white text-sm rounded-lg shadow-lg whitespace-nowrap"
         x-cloak>
      Create new entry
      <div class="absolute top-1/2 -right-1 transform -translate-y-1/2 w-2 h-2 bg-gray-900 dark:bg-gray-700 rotate-45"></div>
    </div>
  </div>
  @endif

  {{-- Footer --}}
  <footer class="bg-white dark:bg-gray-800 shadow">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <p class="text-center text-sm text-gray-500 dark:text-gray-400">
        © {{ new Date().getFullYear() }} Victor Miti.
      </p>
    </div>
  </footer>

  {{-- Page specific scripts --}}
  @stack('scripts')
</body>
</html>
